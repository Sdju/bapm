# Version

> **UNSTABLE:** Early public release. APIs and on-disk layouts may change without a major bump. Not production-ready.

Formats and prints product name + version.

## Public API

- `createVersion(deps)` — returns `{ format(), print() }`
- Types: `VersionDeps`, `VersionApi`
