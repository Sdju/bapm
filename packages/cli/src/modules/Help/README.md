# Help

> **UNSTABLE:** Early public release. APIs and on-disk layouts may change without a major bump. Not production-ready.

Formats and prints CLI usage text.

## Public API

- `createHelp(deps)` — returns `{ format(), print() }`
- Types: `HelpDeps`, `HelpApi`
