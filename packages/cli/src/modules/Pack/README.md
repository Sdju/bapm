# Pack

CLI module for `bapm pack` — thin wrapper over `@bapm/core` `runPack` / `checkReleaseTag`.

## Public API

- `createPack(deps)` → `{ run, formatHelp }`
- Flags: `--archive`, `--dry-run`, `--check-release`, `--tag`
