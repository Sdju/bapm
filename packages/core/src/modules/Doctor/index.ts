/**
 * Doctor — basics: git on PATH, readable manifest/lock, modules dir sanity.
 */

export type { DoctorResult, RunDoctorOptions, DoctorCheck } from "./types.ts";
export { runDoctor, doctor, checkDoctor } from "./runDoctor.ts";
