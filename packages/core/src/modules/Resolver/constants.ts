/** Default modules cache directory (APM wire parity). */
export const APM_MODULES_DIR = "apm_modules";

/** OpenAPM / APM default max resolve depth (req-rs-006). */
export const MAX_RESOLVE_DEPTH = 50;

/** Default parallel download concurrency (APM `--parallel-downloads` default). */
export const DEFAULT_PARALLEL_DOWNLOADS = 4;
