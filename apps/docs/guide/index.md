# Introduction

bapm (Better APM) — dependency manager for AI agent configuration.

Declare agentic dependencies once; install reproducibly into the **cursor-only** host matrix via target packages (`bapm-target-*`). Multi-client adapters are not shipped in-tree today.

OpenAPM wire claims vs APM product CLI: [Conformance & OpenAPM boundary](/guide/conformance).
